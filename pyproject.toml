[project]
name = "enterprise-ai-deployment-lab"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv]
package = true

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
